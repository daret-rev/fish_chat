# FishChat CyberTraining System 🎣

## Описание
Проект создан для интерактивного обучения школьников и студентов распознаванию мошеннических сообщений. Система помогает развить навыки критического мышления через практические тренировки и тестирования.

**Возможности системы:**
- 🎯 **Тренировки** с объяснением ошибок
- 🧪 **Тестирования** с системой опыта (XP)
- 📊 **Статистика** прогресса для учеников
- 👨‍🏫 **Панель управления** для учителей
- 👥 **Управление группами** и назначение тестов
- 📈 **Аналитика успеваемости** для учителей
- 🔐 **Административная консоль** для управления

## Для кого этот проект?
- **Школы** — обучение кибербезопасности на уроках информатики
- **Колледжи/ВУЗы** — практические занятия по информационной безопасности
- **Организации** — обучение сотрудников распознаванию фишинга
- **Преподаватели** — создание интерактивных тестов и мониторинг прогресса

---

## 🚀 Быстрый старт

### Способ 1: Docker (рекомендуется)

```bash
# 1. Скачайте проект
git clone https://github.com/daret-rev/fish_chat.git
cd fish_chat

# 2. Запустите (нужен установленный Docker и Docker Compose)
docker-compose up -d --build

# 3. Проверьте статус контейнеров
docker-compose ps

# Система будет доступна через 1-2 минуты: http://ваш-сервер:5000
```
### Способ 2: Ручная установка
```bash
# 1. Установите Python 3.8 или новее
# Скачать: https://www.python.org/downloads/

# 2. Скачайте проект
git clone https://github.com/daret-rev/fish_chat.git
cd fish_chat

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Запустите систему
python app.py

# Откройте в браузере: http://localhost:5000
```
**Важно:** Перед запуском рекомендуется установить ключ шифрования (app.secret_key в файле app.py) на сложное значение.
---

## 📋 Системные требования

### Сервер (развёртывание):
- **ОС:** Linux (Ubuntu 20.04+, Debian 11+), Windows Server 2019+
- **Рабочие станции (тестирование):** Windows 10/11 (64-bit), macOS 10.15+, Linux (Ubuntu 18.04+, Fedora 32+, CentOS 8+)
- **Процессор:** 2+ ядра
- **Оперативная память:** 4+ ГБ
- **Свободное место на диске:** 2+ ГБ
- **Дополнительно:** Установленный Docker и Docker Compose (для рекомендуемого способа развертывания)
- **Сетевой доступ:** Открытый порт 5000 (или другой, указанный в конфигурации) для доступа по сети

---

## ⚙️ Первоначальная настройка

После успешного запуска выполните:

1. **Определение сетевого адреса:** Определите внутренний IP-адрес сервера (например, `http://192.168.1.100:5000`). Можно настроить DNS-запись `fishchat.school.local` (необязательно).

2. **Вход в систему:** Учётная запись администратора:
   - **Логин:** `admin`
   - **Пароль:** `admin_admin`
   
   **Важно:** Смените пароль при первом входе через консоль администратора (`/cons`).

3. **Настройка ролей:** Зарегистрируйте учителей (или они могут сделать это самостоятельно через форму регистрации) и назначьте им роль «учитель» через консоль управления пользователями.

4. **Регистрация учеников и формирование групп:** Ученики регистрируются самостоятельно. Создайте группы (например, «9А», «10Б-инф») и добавьте в них учеников. Рекомендуется использовать понятные логины (ФИО) и названия групп для удобства навигации.

---

## 🛠️ Обслуживание и администрирование
### Резервное копирование
```bash
cp app.db app.db.backup_$(date +%Y%m%d)
```
Рекомендуется выполнять регулярно.

### Обновление платформы
```bash
docker-compose down
git pull origin main
docker-compose up -d --build
```

### Мониторинг и экспорт данных
- Администратор: отслеживание активности, экспорт CSV через `/cons`
- Учителя: статистика успеваемости через панель управления

---

## 🎯 Рекомендации по эксплуатации в учебном процессе
- Проведение вводного занятия: Учителю рекомендуется провести первое занятие, на котором ученики зарегистрируются в системе и пройдут пробную тренировку.

- Назначение тестов: Тестирования следует назначать на конкретные учебные периоды, контролируя их статус 
активно
/
завершено
активно/завершено.

- Анализ результатов: Регулярный анализ статистики позволяет выявлять общие проблемные темы для повторного разбора с классом и индивидуальные трудности учеников.

---

## 📁 Структура проекта
```
fish_chat/
├── app.py # Основное приложение Flask
├── requirements.txt # Зависимости Python
├── docker-compose.yml # Конфигурация Docker Compose
├── Dockerfile # Конфигурация Docker образа
├── static/ # Статические файлы
├── templates/ # HTML-шаблоны
├── image/ # Изображения проекта
├── app.db # База данных SQLite
├── .gitattributes # Настройки Git
├── .gitignore # Игнорируемые файлы Git
├── pyproject.toml # Конфигурация Python проекта
├── uv.lock # Lock-файл менеджера зависимостей uv
└── README.md # Документация
```
**Основные файлы:**
- `app.py` - главный файл приложения Flask
- `app.db` - база данных SQLite (создаётся автоматически при первом запуске)
- `requirements.txt` - зависимости Python
- `docker-compose.yml` - конфигурация для запуска через Docker Compose
- `Dockerfile` - инструкции для сборки Docker образа

---

## 🔐 Безопасность

- **Обязательная смена пароля по умолчанию** при первом входе
- **Установите сложный `app.secret_key`** в файле `app.py`
